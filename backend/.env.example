SECRET_KEY=dev-secret-key-change-in-production
DEBUG=True
DATABASE_URL=postgres://alhabeed:alhabeed@postgres:5432/alhabeed
ALLOWED_HOSTS=localhost,127.0.0.1,backend
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
JWT_SECRET=dev-jwt-secret
FRONTEND_URL=http://localhost:3000
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
DEFAULT_FROM_EMAIL=noreply@localhost

# Live score sync (football-data.org)
# Get a free API token at https://www.football-data.org/
FOOTBALL_DATA_API_TOKEN=
# Optional default competition code (World Cup = WC)
FOOTBALL_DATA_COMPETITION_CODE=WC
# Required in production: protects /api/cron/* (cron-job.org sends Authorization: Bearer <CRON_SECRET>).
CRON_SECRET=
# Optional ISO dates; cron no-ops outside this window (defaults: no gating)
LIVE_SCORE_SYNC_START=2026-06-11
LIVE_SCORE_SYNC_END=2026-07-19

# Web Push (optional — generate with: python -c "from py_vapid import Vapid; v=Vapid(); v.generate_keys(); print(v.public_key); print(v.private_key)")
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_ADMIN_EMAIL=noreply@alhabeed.com
