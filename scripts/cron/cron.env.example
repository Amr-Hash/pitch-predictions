# Copy to cron.env on the cron host (same directory as the cron scripts).
# chmod 600 cron.env
#
# CRON_SECRET must match the Vercel API env var (used by all cron scripts).

CRON_SECRET=your-random-secret-matching-vercel-CRON_SECRET
ALHABEED_API_URL=https://alhabeed-api.vercel.app
