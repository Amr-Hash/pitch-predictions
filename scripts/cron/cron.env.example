# Copy to cron.env (same directory). chmod 600 cron.env
# CRON_SECRET must match the Vercel API env var and cron-job.org job headers.

CRON_SECRET=your-random-secret-matching-vercel-CRON_SECRET
ALHABEED_API_URL=https://alhabeed-api.vercel.app
